class ExpensesItemModel {
  final String image, title, date, price;

  const ExpensesItemModel(
      {required this.image,
      required this.title,
      required this.date,
      required this.price});
}
